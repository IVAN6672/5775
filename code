import random
import telebot

token = (token)

@bot.message_handler(commands=['help'])
def send_welcome(message):
  bot.reaply_to(message, "hi there have any commands start - start bot")



bot.poling()
